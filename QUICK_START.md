# Quick Start Guide - สร้าง Users ทดสอบ

## 🚀 ขั้นตอนสร้าง Users สำหรับทดสอบ

### 1. เข้า Supabase Dashboard

ไปที่ [https://supabase.com/dashboard](https://supabase.com/dashboard) และเข้าโปรเจคของคุณ

### 2. สร้าง User ใน Authentication

1. คลิกเมนู **Authentication** → **Users** (ด้านซ้าย)
2. คลิกปุ่ม **"Add user"** → **"Create new user"**
3. กรอกข้อมูล:
   - **Email:** `staff@anajak.com`
   - **Password:** `password123`
   - **Auto Confirm User:** ✅ เปิด (สำคัญ!)
4. คลิก **"Create user"**
5. **COPY User UID** (จะอยู่ในคอลัมน์ UID)

### 3. เพิ่มข้อมูลพนักงานในตาราง employees

1. คลิกเมนู **SQL Editor**
2. คลิก **"New query"**
3. Paste SQL นี้ (แทนที่ `USER_UID_ที่คัดลอกมา` ด้วย UID จริง):

```sql
-- แทนที่ USER_UID ด้วย UID จริงที่คัดลอกมา
INSERT INTO employees (id, name, email, phone, role, base_salary_rate, ot_rate_1_5x, ot_rate_2x) 
VALUES 
  ('USER_UID_ที่คัดลอกมา', 'พนักงานทดสอบ', 'staff@anajak.com', '0812345678', 'staff', 20000, 1.5, 2.0);
```

4. คลิก **"Run"** (หรือกด Ctrl+Enter)

### 4. ทดสอบ Login

1. กลับไปที่ [http://localhost:3001/login](http://localhost:3001/login)
2. Login ด้วย:
   - Email: `staff@anajak.com`
   - Password: `password123`
3. ✅ ควรเข้าสู่ระบบได้!

---

## 👥 สร้าง Users เพิ่มเติม (Admin & Supervisor)

ทำซ้ำขั้นตอนเดียวกัน แต่เปลี่ยน role:

### Admin User

```sql
-- User: admin@anajak.com / password123
INSERT INTO employees (id, name, email, phone, role, base_salary_rate, ot_rate_1_5x, ot_rate_2x) 
VALUES 
  ('ADMIN_USER_UID', 'ผู้ดูแลระบบ', 'admin@anajak.com', '0812345678', 'admin', 30000, 1.5, 2.0);
```

### Supervisor User

```sql
-- User: supervisor@anajak.com / password123
INSERT INTO employees (id, name, email, phone, role, base_salary_rate, ot_rate_1_5x, ot_rate_2x) 
VALUES 
  ('SUPERVISOR_USER_UID', 'หัวหน้างาน', 'supervisor@anajak.com', '0823456789', 'supervisor', 25000, 1.5, 2.0);
```

---

## 🎯 สร้างหลาย Users พร้อมกัน

```sql
-- แทนที่ UID ทั้ง 3 ตัว
INSERT INTO employees (id, name, email, phone, role, base_salary_rate, ot_rate_1_5x, ot_rate_2x) 
VALUES 
  ('ADMIN_UID', 'Admin User', 'admin@anajak.com', '0812345678', 'admin', 30000, 1.5, 2.0),
  ('SUPERVISOR_UID', 'Supervisor User', 'supervisor@anajak.com', '0823456789', 'supervisor', 25000, 1.5, 2.0),
  ('STAFF_UID', 'Staff User 1', 'staff@anajak.com', '0834567890', 'staff', 20000, 1.5, 2.0),
  ('STAFF2_UID', 'Staff User 2', 'staff2@anajak.com', '0845678901', 'staff', 20000, 1.5, 2.0);
```

---

## ⚠️ สิ่งที่ต้องระวัง

1. **ต้อง Create User ใน Authentication ก่อน** แล้วค่อย INSERT ใน employees
2. **UID ต้องตรงกัน** ระหว่าง Auth User กับ employees.id
3. **Auto Confirm User ต้องเปิด** ไม่งั้นต้องไปยืนยัน email
4. **Email ต้องไม่ซ้ำ** ใน Supabase Auth

---

## 🐛 แก้ปัญหา

### ❌ "Invalid login credentials"
- ตรวจสอบ email/password ถูกต้องหรือไม่
- ตรวจสอบว่า Auto Confirm User เปิดแล้ว

### ❌ "ไม่พบข้อมูลพนักงาน"
- ตรวจสอบว่าได้ INSERT ข้อมูลใน `employees` แล้ว
- ตรวจสอบว่า `id` ใน employees ตรงกับ User UID

### ❌ "User not found"
- User อาจถูกลบหรือยังไม่ได้สร้าง
- ลองสร้าง user ใหม่

---

## 📱 ขั้นตอนถัดไป

หลังจาก Login สำเร็จแล้ว:

1. ✅ ทดสอบ **เช็กอิน** (ต้องอนุญาต Camera + GPS)
2. ✅ ทดสอบ **เช็กเอาท์**
3. ✅ ทดสอบ **ขอ OT**
4. ✅ Login ด้วย Admin/Supervisor → ไปที่ `/admin` เพื่ออนุมัติ OT
5. ✅ ดู **ประวัติ** และ **รายงาน**

---

**หากยังมีปัญหา:**
- เช็ค Console (F12) ในเบราว์เซอร์
- ดู Logs ใน Supabase Dashboard → Logs
- อ่าน `SETUP.md` สำหรับรายละเอียดเพิ่มเติม

**Good luck!** 🚀

