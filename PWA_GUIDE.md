# 📱 คู่มือการใช้งาน PWA - Anajak HR

Progressive Web App (PWA) ได้ถูก setup เรียบร้อยแล้ว! ตอนนี้ผู้ใช้สามารถติดตั้งแอป Anajak HR บนมือถือ iOS และ Android ได้เลย

---

## ✅ สิ่งที่ได้ทำแล้ว

### 1. ติดตั้ง Dependencies
- ✅ `next-pwa` - PWA support สำหรับ Next.js
- ✅ Service Worker สำหรับ offline support

### 2. Configuration Files
- ✅ `public/manifest.json` - PWA manifest file
- ✅ `next.config.js` - PWA configuration
- ✅ `app/layout.tsx` - iOS meta tags และ PWA metadata
- ✅ `.gitignore` - ไม่ track PWA generated files

### 3. Icons
- ✅ สร้าง placeholder icons (SVG) ใน `public/icons/`
- ✅ รองรับ 8 ขนาด: 72, 96, 128, 144, 152, 192, 384, 512

---

## 📲 วิธีติดตั้งแอปบนมือถือ

### iOS (iPhone/iPad)

1. **เปิด Safari** (ต้องใช้ Safari เท่านั้น, Chrome ไม่รองรับ)
2. ไปที่เว็บไซต์ Anajak HR
3. กดปุ่ม **แชร์** (Share) ที่ด้านล่าง
4. เลื่อนลงแล้วกด **"เพิ่มที่หน้าจอโฮม"** (Add to Home Screen)
5. กด **"เพิ่ม"** (Add)
6. ✨ ไอคอนแอปจะปรากฏบนหน้าจอโฮม

### Android

1. เปิด **Chrome** browser
2. ไปที่เว็บไซต์ Anajak HR
3. กดเมนู 3 จุด ที่มุมขวาบน
4. กด **"เพิ่มไปยังหน้าจอหลัก"** (Add to Home screen)
5. กด **"ติดตั้ง"** (Install)
6. ✨ แอปจะถูกติดตั้งเหมือนแอปปกติ

---

## 🚀 การ Deploy

### Vercel (แนะนำ)

```bash
# 1. Build และ test ก่อน
npm run build
npm start

# 2. Push ขึ้น GitHub
git add .
git commit -m "Add PWA support"
git push

# 3. Deploy ผ่าน Vercel
# - ไป vercel.com
# - เชื่อมต่อกับ GitHub repo
# - Deploy (จะ build และ deploy อัตโนมัติ)
```

### คำสั่งที่สำคัญ

```bash
# Development (PWA ปิดอยู่)
npm run dev

# Production Build (PWA เปิดใช้งาน)
npm run build
npm start
```

---

## 🎨 การแทนที่ Icons ด้วยโลโก้จริง

ตอนนี้ใช้ placeholder icons (สีน้ำเงิน + ตัวอักษร "HR") 

### วิธีที่ 1: ใช้เครื่องมือออนไลน์ (แนะนำ)

1. ไปที่ https://realfavicongenerator.net
2. อัพโหลดโลโก้ของบริษัท (ขนาดอย่างน้อย 512x512px)
3. Download ไฟล์ทั้งหมด
4. แทนที่ไฟล์ใน `public/icons/`

### วิธีที่ 2: ใช้ ImageMagick

```bash
# ติดตั้ง ImageMagick
brew install imagemagick

# รัน script
./scripts/generate-icons.sh
```

### วิธีที่ 3: สร้างเอง

ดูคำแนะนำใน `public/icons/README.md`

---

## ⚙️ Features ที่ได้จาก PWA

### ✅ ที่ทำงานแล้ว

- 📱 **ติดตั้งบนหน้าจอโฮม** - ใช้งานเหมือนแอป native
- 🎨 **Splash Screen** - หน้าจอ loading เมื่อเปิดแอป
- 📴 **Offline Support** - ใช้งานได้บางส่วนแม้ไม่มีเน็ต (Service Worker)
- 🔄 **Auto Update** - อัพเดทอัตโนมัติเมื่อมีเวอร์ชันใหม่
- 🎯 **Full Screen** - เปิดเต็มจอไม่มี browser bar
- ⚡ **Fast Loading** - โหลดเร็วขึ้นด้วย caching

### 🚧 ข้อจำกัดบน iOS

- 📬 **Push Notifications** - ต้องใช้ iOS 16.4+ (ยังไม่ได้ implement)
- 💾 **Storage** - จำกัด 50MB (เพียงพอสำหรับ HR app)
- 🔔 **Badge** - App badge ไม่รองรับ

---

## 🧪 การทดสอบ PWA

### Test ใน Development

```bash
# 1. Build production
npm run build

# 2. Start production server
npm start

# 3. เปิด browser ไปที่
http://localhost:3000

# 4. เช็ค PWA
# - เปิด Chrome DevTools
# - ไปที่ "Application" tab
# - ดูที่ "Manifest" และ "Service Workers"
```

### Test บนมือถือจริง

1. Deploy ขึ้น production (Vercel/Netlify)
2. เปิดด้วยมือถือ
3. ลองติดตั้ง "Add to Home Screen"
4. ทดสอบการใช้งานทั้งแบบ online และ offline

---

## 🐛 Troubleshooting

### ไม่แสดงปุ่ม "Add to Home Screen"

**iOS:**
- ✅ ต้องใช้ **Safari** เท่านั้น (ไม่ใช่ Chrome)
- ✅ เว็บต้องเป็น **HTTPS** (localhost ใช้ได้)
- ✅ ต้องมี **manifest.json** ที่ถูกต้อง

**Android:**
- ✅ ต้องใช้ **Chrome** (หรือ browser ที่รองรับ)
- ✅ เว็บต้องเป็น **HTTPS**
- ✅ ต้อง visit เว็บอย่างน้อย **2 ครั้ง** ห่างกัน 5 นาที

### Service Worker ไม่ทำงาน

```bash
# ลบ cache และ rebuild
rm -rf .next
npm run build
npm start
```

### Icons ไม่แสดง

1. เช็คว่าไฟล์อยู่ใน `public/icons/`
2. เช็ค manifest.json ว่า path ถูกต้อง
3. Hard refresh (Cmd+Shift+R บน Mac)

---

## 📚 Resources

- [Next PWA Documentation](https://github.com/shadowwalker/next-pwa)
- [Web.dev PWA Guide](https://web.dev/progressive-web-apps/)
- [iOS PWA Capabilities](https://firt.dev/ios-17.4)
- [PWA Assets Generator](https://realfavicongenerator.net)

---

## 🎉 ขั้นตอนถัดไป

1. **แทนที่ Icons** - ใช้โลโก้จริงของบริษัท
2. **เพิ่ม Screenshots** - สำหรับแสดงใน App Gallery
3. **ทดสอบ** - ทดสอบบนอุปกรณ์จริงทั้ง iOS และ Android
4. **Deploy** - Deploy ขึ้น production
5. **แจ้งพนักงาน** - สอนวิธีติดตั้งแอป

---

## 💡 Tips

- PWA ใช้งานได้ดีสำหรับ HR app ที่ไม่ต้องการ native features ซับซ้อน
- ถ้าต้องการ Push Notifications แบบเต็มรูปแบบ อาจต้องใช้ Capacitor แทน
- ควรมี "Install Guide" บนหน้า login สอนพนักงานวิธีติดตั้ง
- ตรวจสอบ analytics ว่ามีคนใช้ผ่าน PWA กี่เปอร์เซ็นต์

---

**🎊 Setup PWA สำเร็จแล้ว! พนักงานสามารถติดตั้งแอป Anajak HR บนมือถือได้เลย**

