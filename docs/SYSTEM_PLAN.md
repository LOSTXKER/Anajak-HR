# แผนระบบบันทึกเข้างาน + OT ผ่านมือถือ (Mobile Attendance System)

## 1. เป้าหมายระบบ
- บันทึกเวลาเข้างาน–เลิกงานของพนักงานแบบ Real-time
- มีระบบขอ OT / อนุมัติ OT / คำนวณชั่วโมง OT อัตโนมัติ
- มีระบบยืนยันตัวตน ป้องกันการฝากเช็กอินแทนกัน
- ใช้แค่มือถือ (พนักงาน + หัวหน้า) ไม่ต้องมีเครื่องสแกนหน้า/สแกนนิ้วที่ร้าน
- สร้างข้อมูลสรุปสำหรับคิดเงินเดือนและวิเคราะห์พนักงาน

---

## 2. กลุ่มผู้ใช้หลัก (User Role)
1. **พนักงาน (Staff)**
   - เช็กอินเข้างาน / เช็กเอาท์เลิกงาน
   - ขอทำ OT
   - ดูประวัติการเข้างานและ OT ของตัวเอง

2. **หัวหน้างาน / แอดมินสาขา (Supervisor)**
   - อนุมัติ/ไม่อนุมัติคำขอ OT
   - ดูรายชื่อพนักงานที่เข้างาน/ยังไม่เข้างาน/กำลังทำ OT
   - ดูรายงานการเข้างานและ OT ของทีม

3. **เจ้าของกิจการ / HR / การเงิน (Owner/HR/Finance)**
   - ดูรายงานรวมทั้งเดือน ทั้งบริษัท
   - ดาวน์โหลดรายงานไปใช้คำนวณเงินเดือน
   - วิเคราะห์คนมาสาย ขาดงาน OT เยอะ

---

## 3. ฟีเจอร์หลัก (MVP)

### 3.1 ระบบเช็กอิน / เช็กเอาท์
- พนักงานเปิดแอป/เว็บ/ไลน์บอท แล้วกดปุ่ม **"เข้างาน"**
- ระบบจะ:
  - ดึงตำแหน่ง GPS (ต้องอยู่ในรัศมีที่กำหนด เช่น 50–100 เมตรจากร้าน)
  - ให้ถ่ายรูปหน้า (Face Selfie)
  - บันทึกเวลา (timestamp)
- ตอนเลิกงาน ทำขั้นตอนคล้ายกันด้วยปุ่ม **"เลิกงาน"**

**ข้อมูลที่บันทึกในแต่ละ Log**
- employee_id
- clock_in_time / clock_out_time
- clock_in_gps / clock_out_gps
- clock_in_photo / clock_out_photo
- หมายเหตุ (optional)

---

### 3.2 ระบบขอ OT

**Flow การขอ OT**
1. พนักงานกดปุ่ม **"ขอ OT"**
2. กรอกข้อมูล:
   - เวลาที่จะทำ OT (เริ่ม–สิ้นสุด)
   - เหตุผล
   - ถ่ายรูปหน้างาน (Before OT)
3. ส่งคำขอ → สถานะเริ่มต้น = `pending`
4. หัวหน้ารับแจ้งเตือน → กด `อนุมัติ` หรือ `ไม่อนุมัติ`
5. ถ้าอนุมัติ → ระบบล็อกเวลา OT ตามที่ขอ/อนุมัติไว้
6. เมื่อทำ OT เสร็จ → พนักงานกดปุ่ม **"จบ OT"** + ถ่ายรูปหลังทำ (After OT)

**ข้อมูลที่บันทึกใน `ot_requests`**
- employee_id
- requested_start_time / requested_end_time
- approved_start_time / approved_end_time
- status (pending / approved / rejected / completed)
- before_photo / after_photo
- ot_hours (คำนวณภายหลัง)

---

### 3.2 ระบบ OT (ย้ายไปรวมรายละเอียดเต็มในหัวข้อ 21)
(สรุปสั้น ๆ — รายละเอียดทั้งหมดดูหัวข้อ 21 ด้านล่าง)
 (ภาพรวมเบื้องต้น)
- พนักงานสามารถขอ OT ล่วงหน้า โดยระบุเวลาเริ่ม–จบ และเหตุผล
- หัวหน้าต้องอนุมัติก่อน จึงจะนับเป็น OT ที่จ่ายเงินได้
- เมื่อทำ OT จริง พนักงานต้องกด "เริ่ม OT" และ "จบ OT" พร้อมถ่ายรูปยืนยัน
- ระบบคำนวณชั่วโมง OT ตามเวลาที่อนุมัติจริง และส่งเข้าโมดูลเงินเดือน

### ประเภท OT
- OT วันทำงานปกติ (หลังเลิกงานปกติ) — OT Rate ปกติ เช่น 1.5x
- OT ก่อนเข้างาน (เช้าเกินเวลา) — สามารถกำหนดว่าจะจ่ายหรือไม่จ่ายได้ใน Rule
- OT วันหยุด/นักขัตฤกษ์ — ใช้ OT Rate สูงกว่า เช่น 2x หรือมากกว่านั้น ตามที่ HR ตั้ง

### หลักการกันโกง
- OT ต้องมี `approved_by` ก่อนเท่านั้นถึงจะเข้าไปคิดเงิน
- ถ้าไม่มีคำขอ OT แต่พนักงานอยู่เกินเวลา → ระบบแสดงเป็น "อยู่เกินเวลา" แต่ไม่ถือเป็น OT จนกว่าจะมีการอนุมัติย้อนหลังโดยหัวหน้า
- บังคับให้กด "เริ่ม OT" / "จบ OT" พร้อมถ่ายรูป เพื่อยืนยันว่าทำอยู่จริง

## 3.3 ระบบยืนยันตัวตน (Anti-Cheat)

1. **Face Selfie + Face Template (ง่ายสุดเริ่มต้น)**
   - ตอนสมัครพนักงาน จะเก็บรูปโปรไฟล์ 1 รูปเป็น baseline
   - ตอนเช็กอิน/เช็กเอาท์ ต้องถ่ายรูปหน้าใหม่ทุกครั้ง
   - ระยะแรกอาจให้หัวหน้าดูเทียบเองจาก Dashboard (MVP)
   - เวอร์ชันหลังๆ ค่อยต่อ API Face Recognition เพื่อเทียบอัตโนมัติ

2. **GPS Fence**
   - กำหนด Location ร้าน/โรงงานเป็นจุดกลาง
   - ตั้งรัศมี เช่น 100 เมตร → ถ้าเช็กอินนอกพื้นที่ = ไม่ให้เช็กอิน

3. **Device Binding (ถ้าต้องการกันฝากเพื่อนล็อกอิน)**
   - ผูก account กับ device_id / เบอร์โทร / ไลน์
   - ถ้า login จากเครื่องใหม่ให้แจ้งแอดมินอนุมัติ

---

### 3.4 Dashboard สำหรับหัวหน้า & เจ้าของ

**หัวหน้างานเห็น:**
- รายชื่อพนักงาน + สถานะวันนี้
  - เข้างานแล้ว (เวลาเท่าไหร่)
  - ยังไม่เข้างาน
  - เลิกงานแล้ว
  - กำลังทำ OT
- Card แสดงจำนวนคน:
  - เข้างานปกติ
  - มาสาย
  - ขาดงาน
- รายการคำขอ OT + ปุ่มอนุมัติ/ไม่อนุมัติ

**เจ้าของ/HR เห็น:**
- สรุปชั่วโมงทำงานต่อคน ต่อเดือน
- สรุปชั่วโมง OT ต่อคน ต่อเดือน
- รายงานมาสาย/ขาดงาน
- ปุ่ม Export เป็น Excel/CSV

---

## 4. โครงสร้างฐานข้อมูล (คร่าวๆ)

### 4.1 ตาราง `employees`
- id (PK)
- name
- phone
- role (staff/supervisor/admin)
- base_salary_rate (optional)
- ot_rate_1x, ot_rate_1_5x, ot_rate_2x (optional)
- face_profile_image_url
- device_id (optional)
- created_at

### 4.2 ตาราง `attendance_logs`
- id (PK)
- employee_id (FK → employees)
- work_date
- clock_in_time
- clock_in_gps_lat
- clock_in_gps_lng
- clock_in_photo_url
- clock_out_time
- clock_out_gps_lat
- clock_out_gps_lng
- clock_out_photo_url
- total_hours
- is_late (bool)
- status (present/absent/leave)
- note
- created_at

### 4.3 ตาราง `ot_requests`
- id (PK)
- employee_id (FK)
- request_date
- requested_start_time
- requested_end_time
- approved_start_time
- approved_end_time
- status (pending/approved/rejected/completed)
- before_photo_url
- after_photo_url
- ot_hours
- approved_by (FK → employees หรือ admins)
- created_at

### 4.4 ตาราง `payroll_summaries` (สำหรับสรุปเงินเดือน)
- id (PK)
- employee_id (FK)
- period_start_date
- period_end_date
- total_work_hours
- total_ot_hours
- total_late_minutes
- total_absent_days
- base_pay_amount
- ot_pay_amount
- total_pay_amount
- created_at

---

## 5. เทคโนโลยีที่แนะนำ

### Backend
- Supabase / Firebase / NestJS + PostgreSQL
- ฟีเจอร์ที่ต้องมี:
  - Auth (Login/Role based)
  - REST API / GraphQL สำหรับ Mobile/Web
  - Row-Level Security (ให้เห็นเฉพาะข้อมูลตัวเอง ยกเว้นหัวหน้า/แอดมิน)

### Frontend / Mobile
- แบบ Web App: Next.js + PWA (เปิดผ่านเบราว์เซอร์ก็เหมือนแอป)
- หรือทำเป็น LINE OA + LIFF App เพื่อให้พนักงานใช้ผ่านไลน์ (สะดวก ไม่ต้องลงแอปใหม่)

### ฟีเจอร์มือถือ
- ขอ Permission: Camera, Geolocation, Push Notification

---

## 6. Roadmap การพัฒนา (แบ่งเฟส)

### Phase 1 – MVP (ใช้งานได้จริงแบบง่าย)
- ระบบสมัครพนักงาน + Login
- ระบบเช็กอิน/เช็กเอาท์ + GPS + ถ่ายรูป
- บันทึก attendance_logs
- Dashboard ง่ายๆ ให้หัวหน้าดูว่าใครเข้างานแล้ว
- Export Excel รายวัน/รายเดือน

### Phase 2 – OT & Supervisor
- ฟีเจอร์ขอ OT + แนบรูปก่อนทำ
- หัวหน้าอนุมัติ/ไม่อนุมัติ OT
- ปุ่มจบ OT + แนบรูปหลังทำ
- คำนวณ ot_hours อัตโนมัติ
- สรุป OT รายคน / รายเดือน

### Phase 3 – ระบบเงินเดือน & Report ลึกขึ้น
- สรุปชั่วโมงทำงานปกติ + OT
- ตั้ง OT Rate (1.5x / 2x)
- คำนวณยอดเงิน (base + OT)
- Export ไฟล์สำหรับส่งให้ฝ่ายบัญชี

### Phase 4 – Anti-Cheat & Automation เพิ่มเติม
- Face Recognition API (เทียบใบหน้าอัตโนมัติ)
- แจ้งเตือนผ่านไลน์/แจ้งเตือนมือถือ
  - เมื่อลืมเช็กเอาท์
  - เมื่อมีคำขอ OT ใหม่
  - เมื่อมาสาย
- ระบบลางาน (ลากิจ/ลาป่วย/ลาพักร้อน)

---

## 7. กติกา/นโยบายการใช้งาน (ควรเขียนชัด ๆ ให้พนักงานรู้)
- เช็กอินต้องอยู่ในพื้นที่ร้านเท่านั้น
- ห้ามฝากเพื่อนเช็กอินแทน ถ้าจับได้มีโทษยังไง
- การขอ OT ต้องขอก่อนทำ และต้องผ่านการอนุมัติ
- การลืมเช็กอิน/เช็กเอาท์ แก้ไขได้โดยหัวหน้าตรวจหลักฐานแล้วแก้ให้ในระบบ

---

## 8. ขั้นตอนถัดไป
1. เคาะให้ชัดว่าอยากเริ่มแบบ **Web App / LINE OA / Mobile App จริงจัง**
2. ลิสต์จำนวนพนักงาน และสาขา (ถ้ามีหลายที่)
3. วาง Flow การใช้งานรายวันแบบละเอียดเป็น Step
4. เอา Requirement นี้ไปให้ Dev ทำ UX/UI Wireframe
5. เริ่ม Dev ตาม Phase ที่วางไว้


---

## 9. การใช้งานร่วมกับ LINE Rich Menu + Web App

### 9.1 รูปแบบโดยรวม
- ระบบหลักเป็น **Web App** (เช่น Next.js) ทำงานได้บนมือถือและคอม
- ให้พนักงานเข้าใช้งานผ่าน **LINE Official Account** โดยใช้ **Rich Menu** เป็นจุดกดเข้า
- ใช้ LINE Login / LIFF เป็นตัวยืนยันตัวตนพนักงาน แล้วโยงกับ `employee_id` ในฐานข้อมูล

### 9.2 Flow การใช้งานของพนักงาน
1. แอด LINE OA ของบริษัท
2. กดปุ่มใน Rich Menu เช่น "เข้างาน", "เลิกงาน", "ขอ OT", "ประวัติ"
3. LINE เปิด Web App ผ่าน in-app browser / LIFF
4. Web App ใช้ token จาก LINE เพื่อระบุตัวตน → map ไปยัง `employees`
5. ระบบขอสิทธิ์ใช้งานกล้อง + GPS → ให้พนักงานเช็กอิน/เอาท์/ขอ OT ตาม flow ในข้อ 3

### 9.3 โครง Rich Menu (ตัวอย่าง)
ใช้รูปแบบ 2 แถว 3 ปุ่ม (6 ปุ่ม):

- แถวบน (งานประจำวัน)
  - ปุ่ม 1: 🕒 เข้างาน → URL: `/checkin`
  - ปุ่ม 2: 🚪 เลิกงาน → URL: `/checkout`
  - ปุ่ม 3: 🔥 ขอ OT → URL: `/ot/request`

- แถวล่าง (ข้อมูลและอื่น ๆ)
  - ปุ่ม 4: 📊 ประวัติ → URL: `/history`
  - ปุ่ม 5: 📅 ลางาน (ถ้ามีในเฟสถัดไป) → URL: `/leave`
  - ปุ่ม 6: ☎️ ติดต่อ/FAQ → URL: `/help`

> หมายเหตุ: URL จริงใน LINE จะเป็นแบบเต็ม เช่น `https://app.yourdomain.com/checkin` หรือ LIFF URL ที่ชี้ไปยัง path เหล่านี้

### 9.4 Authentication ผ่าน LINE
- ใช้ LINE Login / LIFF ดึงข้อมูล `userId`, `displayName`, `pictureUrl`
- ครั้งแรกที่พนักงานเข้า:
  - ให้กรอก "รหัสพนักงาน" หรือ "เบอร์โทร + OTP" เพื่อผูกกับ `employees`
  - หลังจากผูกแล้ว ครั้งต่อ ๆ ไปใช้ LINE userId เชื่อมตรงกับ employee ได้เลย
- ฝั่งหัวหน้า/เจ้าของ:
  - อาจใช้ LINE เหมือนกันแต่กำหนด role เป็น `supervisor` / `admin`
  - หรือมีหน้า login แยกใน Web App สำหรับใช้บนคอมพิวเตอร์

### 9.5 หน้า Web App ฝั่งพนักงาน (ผูกกับ Rich Menu)
- `/checkin`
  - แสดงชื่อพนักงานจาก LINE + ข้อมูลจากฐานข้อมูล
  - ขอสิทธิ์กล้อง + GPS
  - แสดงสถานะโลเคชัน (อยู่ในเขต/นอกเขต)
  - ปุ่ม "ยืนยันเข้างาน" → ยิง API เพื่อบันทึก `attendance_logs`

- `/checkout`
  - แสดงเวลาที่เข้างานวันนี้
  - แสดงชั่วโมงทำงานชั่วคราว
  - ปุ่ม "ยืนยันเลิกงาน" + note → บันทึกเวลาออก + คำนวณ total_hours

- `/ot/request`
  - ฟอร์มขอ OT: วันที่, เวลาเริ่ม–จบ, เหตุผล, อัพโหลด/ถ่ายรูป Before OT
  - ปุ่มส่งคำขอ → บันทึกใน `ot_requests` สถานะ `pending`

- `/ot` (ประวัติ OT)
  - แสดงรายการ OT ทั้งหมดของพนักงาน
  - สถานะ: pending / approved / rejected / completed
  - ถ้าเป็น OT ที่อนุมัติแล้วแต่ยังไม่จบ → มีปุ่มให้กดจบ + ถ่ายรูป After OT

- `/history`
  - แสดงประวัติการเข้างานเป็น list/calendar
  - มองเห็น: เข้างาน/เลิกงานกี่โมง, ชั่วโมงรวม, มาสาย/ขาด

### 9.6 Web App ฝั่งหัวหน้า / Admin
- `/admin/dashboard`
  - Today View: ใครเข้างานแล้ว / มาสาย / ยังไม่มา / กำลังทำ OT
  - กล่องแจ้งเตือนคำขอ OT ที่รออนุมัติ
  - ปุ่มอนุมัติ/ไม่อนุมัติ OT + ดูรายละเอียด/รูป Before

- `/admin/employees`
  - จัดการข้อมูลพนักงาน (เพิ่ม/แก้ไข)
  - ดูว่า LINE userId ไหนผูกกับพนักงานคนไหน

- `/admin/reports`
  - เลือกช่วงเวลา → ดูสรุปชั่วโมงทำงาน / OT / มาสาย / ขาด
  - ปุ่ม Export Excel/CSV สำหรับใช้ทำเงินเดือน

### 9.7 Tech Stack เฉพาะส่วน LINE + Web App
- Frontend:
  - Next.js / React
  - Tailwind CSS
  - ใช้ LIFF SDK (ถ้าเชื่อมกับ LINE)
  - ใช้ Web APIs: `navigator.geolocation`, `navigator.mediaDevices.getUserMedia`

- Backend:
  - Node.js (NestJS/Express) + PostgreSQL หรือ Supabase/Firebase ก็ได้
  - JWT/Session ผูกกับ LINE userId
  - Endpoint หลัก: checkin, checkout, ot request/approve, history, admin dashboard, reports

### 9.8 Roadmap เพิ่มสำหรับ LINE Integration
- **Phase 1 (MVP + LINE)**
  - ทำ Web App: `/checkin`, `/checkout`, `/history`
  - ทำ LINE Login / LIFF + mapping กับ `employees`
  - สร้าง LINE Rich Menu และผูก URL ไปยังหน้าสำคัญ

- **Phase 2 (OT + Admin)**
  - เพิ่ม `/ot/request`, `/ot`
  - ทำ `/admin/dashboard` + `/admin/reports`
  - เพิ่มระบบอนุมัติ OT ผ่านเว็บ (และถ้าต้องการ ผ่าน LINE แจ้งเตือน)

- **Phase 3 (ฟีเจอร์เสริม)**
  - เพิ่มหน้าลางาน `/leave`
  - แจ้งเตือนผ่าน LINE เมื่อมาสาย/ลืมเช็กเอาท์/มี OT ใหม่
  - ต่อ Face Recognition API เพิ่มจากเดิมที่ใช้รูปเฉย ๆ


---

## 10. ระบบลางาน (Leave Management)
### 10.1 ประเภทการลา
- ลาป่วย (แนบใบรับรองแพทย์ได้)
- ลากิจส่วนตัว
- ลาพักร้อน
- ลาอื่น ๆ (กำหนดเองได้)

### 10.2 Flow การลางาน
1. พนักงานเข้า `/leave/request` ผ่าน LINE Rich Menu
2. เลือกประเภทการลา + วันที่ลา + ครึ่งวัน/เต็มวัน
3. ใส่เหตุผล และแนบไฟล์รูปใบรับรองแพทย์ (ถ้ามี)
4. กดส่ง → สถานะเริ่มต้นเป็น `pending`
5. หัวหน้ากดอนุมัติ/ปฏิเสธได้จาก `/admin/leave`
6. เมื่ออนุมัติ → ระบบ mark วันนั้นใน `attendance_logs` เป็น `leave`
7. ระบบคำนวณสิทธิ์ลาที่เหลือแต่ละประเภท

### 10.3 ฐานข้อมูลใหม่: `leave_requests`
- id
- employee_id
- leave_type
- start_date
- end_date
- is_half_day
- reason
- attachment_url
- status (pending/approved/rejected)
- approved_by
- created_at

---

## 11. ระบบกะ / ตารางทำงาน (Shift Scheduling)
### 11.1 สร้างกะทำงาน
- ตั้งชื่อกะ เช่น "กะเช้า", "กะดึก"
- เวลาเริ่ม–เวลาเลิก
- สามารถตั้งเป็น pattern รายสัปดาห์ได้

### 11.2 Assign พนักงานลงกะ
- ในหน้า `/admin/shifts`
- เลือกพนักงาน → ผูกกะประจำ หรือกำหนดเฉพาะบางวัน

### 11.3 การใช้งานร่วมกับ Attendance
- เวลาเช็กอิน จะเทียบกับกะว่าสายหรือไม่
- ถ้าเข้างานนอกเวลาที่กำหนด → mark “สาย” หรือ “เข้างานผิดกะ”
- รายงานรายเดือนสามารถแสดงสรุปตามกะได้

### 11.4 ตารางใหม่: `shifts` และ `employee_shift_assignments`

---

## 12. Rule Engine: กติกาการทำงาน / มาสาย / ขาด / OT
### 12.1 ตั้งค่า Rule กลาง
- มาสายเกิน X นาที = “สาย”
- มาสายเกิน X ครั้ง/เดือน = Highlight ในรายงาน
- ขาดงาน = ไม่เช็กอินทั้งวัน + ไม่แจ้งลา
- OT ต้องมีสถานะ `approved` ก่อนเท่านั้นถึงจะคิดเงินได้
- OT ไม่เกิน X ชั่วโมงต่อวัน/สัปดาห์

### 12.2 การทำงานของระบบ
- ความสาย / ขาดงาน จะถูกประมวลผลอัตโนมัติทุกคืน
- แสดงสี/สัญลักษณ์ใน Dashboard
- ใช้ค่าพวกนี้ในการสรุปเงินเดือนหรือ KPI

---

## 13. ระบบแจ้งเตือน (Notifications)
### 13.1 พนักงาน
- แจ้งเตือนเมื่อใกล้ถึงเวลาเข้างาน แต่ยังไม่เช็กอิน
- แจ้งเตือนเมื่อถึงเวลาเลิกงาน แล้วยังไม่เช็กเอาท์
- แจ้งเตือนเมื่อหัวหน้าอนุมัติ/ไม่อนุมัติ OT หรือการลา

### 13.2 หัวหน้า
- แจ้งเตือนเมื่อมีคำขอ OT, ลางาน
- แจ้งเตือนเมื่อมีพนักงานไม่มาเช็กอินในเวลาที่กำหนด

### 13.3 เทคโนโลยี
- LINE Messaging API (Push Message)
- Webhook แจ้งเตือนผ่าน backend

---

## 14. ระบบแก้ไขเวลาเข้างาน + Audit Log
### 14.1 การแก้ไขเวลา (โดยหัวหน้า/HR เท่านั้น)
- เปิดรายการของวันนั้น
- กดปรับเวลาเข้างาน/เลิกงาน
- ใส่เหตุผลทุกครั้ง เช่น “ลืมกด”, “เน็ตล่ม”

### 14.2 Audit Log
ระบบบันทึกทุกครั้งที่มีการแก้ไข:
- ใครเป็นคนแก้
- เวลาเก่าเป็นอะไร → ใหม่เป็นอะไร
- เหตุผล
- วันที่แก้ไข

ตารางใหม่: `attendance_audit_logs`

---

## 15. ระบบสาขา / หลายโลเคชัน (Multi-Branch)
### 15.1 โครงสร้างสาขา
- ตาราง `branches` เก็บชื่อสาขา + GPS center point
- พนักงานถูกผูกกับสาขาหลัก

### 15.2 การเช็กอิน
- ระบบเทียบ GPS กับสาขาที่พนักงานอยู่
- ถ้าเกินรัศมี → ไม่อนุญาตให้เช็กอิน

### 15.3 รายงานแยกสาขา
- Dashboard แยกตามสาขา
- OT/ขาดงาน/สาย ของแต่ละสาขา

---

## 16. ระบบ Performance / KPI พนักงาน
### 16.1 ค่าที่วัดได้อัตโนมัติ
- % การมาทำงาน (Attendance Rate)
- จำนวนครั้งที่สายต่อเดือน
- จำนวนครั้งที่ขาดงาน
- ชั่วโมง OT ต่อเดือน
- เปรียบเทียบหลายเดือน

### 16.2 ใช้ประโยชน์อะไรได้บ้าง
- ขึ้นเงินเดือน
- ประเมินประจำปี
- คัดเลือกดาวเด่นประจำเดือน

---

## 17. Export / Integration
### 17.1 Export ไปใช้คำนวณเงินเดือน
- Export รายเดือนเป็น Excel / CSV
- แยกตามสาขา / พนักงาน / ประเภทชั่วโมง

### 17.2 Integration อื่น ๆ
- เชื่อมกับ Google Sheets สำหรับ Dashboard เพิ่มเติม
- มี Webhook สำหรับส่งข้อมูลไปยังระบบบัญชี/ERP ภายนอก

---

## 18. Quality of Life ฟีเจอร์เสริม
- Offline Queue: เช็กอินได้แม้เน็ตดับ (sync ภายหลัง)
- Multi-language ไทย/พม่า สำหรับแรงงานต่างชาติ
- หน้า FAQ / Help Center
- การเซ็นยอมรับกติกาการทำงานในครั้งแรก


---

## 19. ระบบวันหยุด (Holiday Management)
### 19.1 ประเภทวันหยุด
- วันหยุดนักขัตฤกษ์ (Public Holiday) — ใช้ร่วมกันทั้งบริษัท
- วันหยุดประจำบริษัท (Company Holiday) — ตั้งเองได้ เช่น วันก่อตั้งบริษัท, ปิดงบ, ปิดปรับปรุง
- วันหยุดเฉพาะสาขา (Branch Holiday) — กรณีบางสาขาปิดเป็นพิเศษ

### 19.2 การจัดการวันหยุด
- หน้า `/admin/holidays` สำหรับ:
  - เพิ่ม/ลบ/แก้ไขวันหยุด
  - เลือกประเภทวันหยุด (Public/Company/Branch)
  - ผูกกับสาขาที่เกี่ยวข้อง (ถ้าเป็น Branch Holiday)
- สามารถ Import วันหยุดนักขัตฤกษ์ประจำปีจากไฟล์/Template ได้

### 19.3 การทำงานร่วมกับ Attendance
- ถ้าวันนั้นเป็นวันหยุดของสาขานั้น:
  - ระบบไม่ถือว่า "ขาดงาน" แม้ไม่มีการเช็กอิน
  - Report แสดงสถานะเป็น `holiday`
- ถ้าพนักงานมาทำงานในวันหยุด:
  - สามารถตั้ง rule ให้คิดชั่วโมงเป็น OT ประเภทพิเศษ (เช่น OT 2x)
  - แสดงในรายงานแยกหมวด "OT วันหยุด"

### 19.4 ตารางฐานข้อมูล `holidays`
- id
- date
- name
- type (public/company/branch)
- branch_id (nullable ถ้าเป็น public/company)
- is_active
- created_at

---

## 20. ระบบ Work From Home (WFH)
### 20.1 Concept
- ให้พนักงานสามารถทำงานจากบ้าน/สถานที่อื่นได้อย่างถูกต้องตามระบบ
- ยังใช้ Flow การเช็กอิน/เช็กเอาท์เหมือนเดิม แต่มีโหมด WFH และกติกา GPS ที่แตกต่างออกไป

### 20.2 การขออนุมัติ WFH
1. พนักงานเข้า `/wfh/request`
2. เลือกวันที่ต้องการ WFH (ทั้งวันหรือครึ่งวัน)
3. ใส่เหตุผล และระบุสถานที่โดยคร่าว (เช่น บ้าน, จังหวัดอื่น)
4. ส่งคำขอ → สถานะเริ่มต้น `pending`
5. หัวหน้าพิจารณาในหน้า `/admin/wfh` แล้วกดอนุมัติ/ปฏิเสธ

### 20.3 การเช็กอินในโหมด WFH
- ถ้าวันนั้นพนักงานได้รับอนุมัติ WFH:
  - ระบบอนุญาตให้เช็กอินได้แม้ไม่อยู่ในรัศมีสาขา
  - ยังใช้การถ่ายรูปหน้า (Selfie) + Timestamp เหมือนเดิม
  - GPS ยังถูกบันทึกไว้ แต่ไม่ใช้ตัดสินว่าอยู่ในเขตสาขา
- แสดงสถานะใน `attendance_logs` เป็น `WFH` แยกจาก `onsite`

### 20.4 การรายงานและวิเคราะห์ WFH
- ในรายงานสามารถแยกได้ว่า:
  - ทำงานจากออฟฟิศกี่วัน
  - WFH กี่วัน
- สามารถใช้ Rule เพิ่มเติม เช่น:
  - จำกัดจำนวนวัน WFH ต่อเดือน
  - ให้เห็นพนักงานที่ WFH บ่อยผิดปกติ

### 20.5 ตารางฐานข้อมูล `wfh_requests`
- id
- employee_id
- date
- is_half_day
- reason
- status (pending/approved/rejected)
- approved_by
- created_at

> หมายเหตุ: ใน `attendance_logs` เพิ่มฟิลด์ `work_mode` เช่น `onsite`, `wfh`, `field` (เผื่อใช้ในอนาคตสำหรับงานนอกสถานที่)


---

## 21. ระบบ OT (รายละเอียดฉบับเต็ม ครบทุก Flow และกติกา)

ระบบ OT นี้ออกแบบให้ครอบคลุมการทำงานจริง ตั้งแต่การขออนุมัติ → ทำงานจริง → จบงาน → คำนวณเงิน → แสดงในรายงาน โดยรองรับทั้งวันปกติ วันหยุด การทำงานนอกพื้นที่ และการกันโกงทุกรูปแบบ

---

### 21.1 ประเภท OT
เพื่อความชัดเจน ระบบควรแยกประเภท OT ตามสถานการณ์:

1. **OT ปกติ (Normal OT)**
   - ทำหลังเวลาเลิกงานปกติ
   - ใช้อัตรา เช่น 1.5 เท่า

2. **OT ก่อนเริ่มงาน (Pre-shift OT)**
   - เข้างานก่อนเวลา
   - HR จะตั้ง rule ได้ว่า จ่ายหรือไม่จ่าย

3. **OT วันหยุด (Holiday OT)**
   - ทำงานในวันหยุด/นักขัตฤกษ์
   - ใช้อัตราพิเศษ เช่น 2x หรือ 3x

4. **OT วันเสาร์–อาทิตย์ (ถ้าแยกจากวันหยุดราชการ)**
   - บริษัทสามารถตั้ง rate เพิ่มเติมได้

ระบบจะเลือกประเภท OT อัตโนมัติจาก **วันนั้นคือวันอะไร + เวลาที่ทำงานจริง**

---

### 21.2 Flow การขอ OT (พนักงาน → หัวหน้า → ระบบ)

#### ขั้นตอนของพนักงาน
1. เปิด LINE Rich Menu → เลือก "ขอ OT"
2. หน้า `/ot/request` ให้กรอก:
   - วันที่ต้องการทำ OT
   - เวลาเริ่ม–เวลาเลิกที่คาดว่าจะทำ
   - เหตุผลของการทำ OT
   - แนบรูปยืนยันหน้างาน (Before OT)
3. ส่งคำขอ → ระบบบันทึกสถานะ `pending`

#### ขั้นตอนของหัวหน้า
1. เปิด `/admin/ot`
2. เห็นรายการ OT ที่ยัง `pending`
3. ตรวจสอบรายละเอียดและรูป
4. กด **อนุมัติ** หรือ **ไม่อนุมัติ**
5. เมื่ออนุมัติ → lock เวลาที่อนุมัติไว้ / ระบุผู้อนุมัติในระบบ

ระบบจะไม่คิด OT ถ้า **ไม่มีการอนุมัติล่วงหน้า**

---

### 21.3 Flow การเริ่มทำ OT จริง
หลังได้รับอนุมัติ:

#### ตอน "เริ่ม OT"
- พนักงานเปิดหน้า `/ot/start/{id}`
- ระบบให้ถ่ายรูป + เก็บ GPS
- Timestamp ถูกบันทึกเป็นเวลาเริ่มจริง

#### ตอน "จบ OT"
- พนักงานเปิดหน้า `/ot/end/{id}`
- ถ่ายรูปหลังทำงานเสร็จ (After OT)
- Timestamp บันทึกเป็นเวลาจบจริง
- ระบบคำนวณชั่วโมง `actual_ot_hours`
- ถ้าชั่วโมงจริงเกินเวลาที่อนุมัติ → ระบบตัดให้เหลือเท่าที่อนุมัติ
- ถ้ามีน้อยกว่า → คิดตามจริง

---

### 21.4 Rule Engine สำหรับ OT

ระบบจะประมวลผลตามกติกาเหล่านี้อัตโนมัติ:

1. **OT ต้องมี approved_by เท่านั้นถึงคิดเงินได้**
2. **OT ไม่เกิน X ชั่วโมงต่อวัน** (ตั้งค่าได้)
3. **OT ไม่เกิน X ชั่วโมงต่อเดือน** (ตั้งค่าได้)
4. **อยู่เกินเวลาแต่ไม่มีคำขอ OT ล่วงหน้า**
   - ขึ้นสถานะ “อยู่เกินเวลา - ไม่ใช่ OT”
   - หัวหน้าต้องอนุมัติย้อนหลังเท่านั้นถึงจะนับเป็น OT ได้
5. **OT วันหยุด**
   - ถ้าเป็นวันหยุดตามระบบ holiday → ชั่วโมงทั้งหมดจะถูกนับเป็น holiday_ot
6. **WFH + OT**
   - Flow เหมือน OT ปกติ แต่จะบันทึก work_mode = `wfh`

---

### 21.5 การคำนวณเงิน OT
สูตรพื้นฐาน:
```
ot_pay = ot_hours × hourly_rate × ot_rate
```

โดย:
- `hourly_rate` = อัตรารายชั่วโมงของพนักงาน (HR คำนวณ/ตั้งเอง)
- `ot_rate` = อัตราคูณของ OT เช่น 1.5, 2, 3
- `ot_hours` = ชั่วโมง OT ที่อนุมัติจริง

ระบบจะรวมยอดเป็น:
```
total_ot_pay = sum(ot_pay ทุกประเภท)
```

และไปบวกกับ **เงินเดือนปกติ** ในมอดูลเงินเดือน

---

### 21.6 การทำงานร่วมกับวันหยุด / ระบบ WFH / ระบบกะ

**วันหยุด:**
- ถ้าเป็นวันหยุด → ทุกชั่วโมง = OT วันหยุด
- Administrator สามารถกำหนด rate สูงเป็นพิเศษได้

**WFH:**
- OT บันทึกเป็น work_mode = wfh
- ยังคงถ่ายรูป/เก็บ GPS และอนุมัติแบบเดียวกับ onsite

**ระบบกะ (Shift):**
- เวลาที่ทำก่อน/หลัง shift จะถูกระบบตรวจว่าเป็น OT หรือไม่
- ถ้าเข้าก่อนกะเกิน X นาที → จะถือเป็น Pre-shift OT หาก rule อนุญาต

---

### 21.7 รายงาน OT (Reports)
ในหน้า `/admin/reports` ระบบจะแสดง:

- ชั่วโมง OT รายวัน/รายเดือน
- รายงานแยกประเภท OT:
  - normal_ot
  - holiday_ot
  - pre_shift_ot
- รวมยอดเงิน OT ต่อพนักงาน
- รวมยอด OT ต่อสาขา
- รายงานพนักงานที่ใช้ OT เกินเกณฑ์ที่ตั้งไว้
- Export เป็น Excel / CSV

---

### 21.8 ตารางฐานข้อมูลที่เกี่ยวข้องกับ OT

#### ตาราง `ot_requests`
- id
- employee_id
- ot_type (normal, holiday, pre_shift)
- requested_start_time
- requested_end_time
- approved_start_time
- approved_end_time
- actual_start_time
- actual_end_time
- approved_by
- before_photo_url
- after_photo_url
- status (pending/approved/rejected/completed)
- actual_ot_hours
- approved_ot_hours
- ot_rate
- ot_amount
- created_at

#### เพิ่มใน `attendance_logs`
- ot_id (nullable)
- work_mode (onsite/wfh)

---

### 21.9 ฟีเจอร์กันโกงใน OT

1. ถ้าไม่ถ่ายรูป → ไม่ให้เริ่ม/จบ OT
2. ถ้า GPS ห่างจากพื้นที่ทำงานเกินกำหนด (ในกรณี onsite) → ห้ามเริ่ม OT
3. ถ้าอยู่เกินเวลาเลิกงานแต่ไม่ทำคำขอ OT → ไม่คิดเงิน
4. ถ้ามีการแก้เวลาเริ่ม/จบ OT → ระบบเก็บ Audit Log ทุกครั้ง
5. มีแจ้งเตือนหัวหน้าเมื่อพนักงานเริ่ม OT ช้า/จบ OT ช้าเกินไป

---

### 21.10 สรุปคุณสมบัติเด่นของระบบ OT เวอร์ชันนี้
- ควบคุมไม่ให้พนักงานทำ OT โดยไม่ขออนุมัติ
- เก็บหลักฐานก่อน–หลังทำงาน เพื่อลดปัญหาการโกง
- คำนวณเวลาจริงแบบอัตโนมัติ + จำกัดชั่วโมงตามกติกาบริษัท
- รองรับวันหยุด, ทำงานนอกสถานที่, WFH
- Export รายงานเงินเดือนครบถ้วน

ระบบนี้ออกแบบให้รองรับทั้งธุรกิจขนาดเล็ก → ใหญ่ โดยยังคงใช้ง่ายผ่าน LINE + Web App



---

## 22. ระบบเงินเดือน Payroll แบบเต็ม (Full Payroll Module)
### 22.1 ข้อมูลพื้นฐานพนักงานด้านเงินเดือน
- เงินเดือนพื้นฐาน (Monthly Salary)
- อัตรารายวัน / รายชั่วโมง (ถ้ามีพนักงานรายวัน)
- ประเภทเงินเดือน: รายเดือน / รายวัน / รายชั่วโมง
- ค่าตำแหน่ง / ค่าเดินทาง / ค่าอาหาร / ค่าเบี้ยเลี้ยง
- เบี้ยขยัน (กำหนดเกณฑ์ได้)
- ประกันสังคม (หัก 5%) + เงินสมทบบริษัท
- หักภาษี ณ ที่จ่าย (optional)

### 22.2 การคำนวณเงินเดือน
ระบบรวมข้อมูลจาก:
- Attendance (จำนวนวันทำงาน)
- มาสาย / ขาดงาน / ลา (หักเงิน)
- OT (ชั่วโมง + rate)
- เบี้ยขยัน (auto calc จาก rule)
- สวัสดิการเพิ่มพิเศษ

### 22.3 Output ที่ระบบต้องสร้างได้
- ใบสลิปเงินเดือน (PDF)
- รายงานสรุปเงินเดือนทั้งบริษัท (Excel / CSV)
- รายงานประกันสังคม (ก.พ.7 / แยกรายพนักงาน)

---

## 23. ระบบประเมินผลพนักงาน (Performance & KPI System)
### 23.1 KPI จากระบบอัตโนมัติ
- % Attendance Rate
- มาสายกี่ครั้งต่อเดือน
- จำนวนวันขาดงาน
- ชั่วโมง OT ต่อเดือน
- คะแนนวินัยอัตโนมัติ (0–100)

### 23.2 KPI จากหัวหน้า
- ประเมินรายไตรมาส/รายเดือน
- ให้คะแนนด้านคุณภาพงาน / ทัศนคติ / ความรับผิดชอบ
- บันทึก Feedback

### 23.3 รายงาน KPI
- เปรียบเทียบแต่ละคน
- ดูเทรนด์การเปลี่ยนแปลงของพนักงาน
- ใช้ประกอบการขึ้นเงินเดือน / โบนัส

---

## 24. ระบบเก็บเอกสารพนักงาน (Employee Document Manager)
### 24.1 ประเภทเอกสาร
- บัตรประชาชน / ทะเบียนบ้าน
- สัญญาจ้างงาน
- ใบเตือน / ใบชมเชย
- ใบลา / ใบแพทย์
- เอกสารประกอบตำแหน่ง

### 24.2 ฟีเจอร์
- อัพโหลดเอกสารผ่านมือถือ
- จัดหมวดหมู่ไฟล์
- ตั้งวันหมดอายุของเอกสาร
- แจ้งเตือนเมื่อถึงกำหนดต่ออายุสัญญา

---

## 25. ระบบจัดกะขั้นสูง (Shift Planner Pro)
### 25.1 UI แบบ Drag & Drop
- ลากพนักงานลงกะบนปฏิทิน
- คัดลอกตารางสัปดาห์ก่อนหน้า
- Template ตารางกะ

### 25.2 Rule อัตโนมัติ
- ไม่ให้จัดกะเกินชั่วโมงแรงงาน
- ตรวจซ้ำซ้อนระหว่างกะ
- แสดงความพร้อมของพนักงาน (availability)

### 25.3 การแจ้งเตือน
- พนักงานได้รับแจ้งเตือนกะใหม่ผ่าน LINE
- แจ้งถ้าตารางกะถูกแก้ไข

---

## 26. ระบบประกาศ HR (Announcement Center)
### 26.1 สำหรับสื่อสารภายในบริษัท
- แจ้งวันหยุด
- แจ้งโบนัสดีๆ
- แจ้งกฎใหม่
- แจ้งเหตุฉุกเฉิน เช่น ไฟดับ / ปรับเวลาเข้างาน

### 26.2 ฟีเจอร์สำคัญ
- ส่งประกาศเข้ากลุ่ม LINE พนักงาน
- เก็บ Log ว่า "ใครอ่านแล้วบ้าง"

---

## 27. ระบบใบเตือน / ใบชมเชย (Disciplinary & Reward System)
### 27.1 ประเภทใบเตือน
- ตักเตือนด้วยวาจา
- ใบเตือนครั้งที่ 1
- ใบเตือนครั้งที่ 2
- ใบเตือนครั้งที่ 3 (ร้ายแรง)

### 27.2 ใบชมเชย
- การปฏิบัติงานโดดเด่น
- ไม่สาย 3 เดือน
- ช่วยงานพิเศษ

### 27.3 ฟีเจอร์
- สร้างเอกสาร + ใส่เหตุผล
- เซ็นหัวหน้า + เซ็นพนักงาน
- ผูกไว้ในแฟ้มพนักงาน
- มีผลต่อ KPI

---

## 28. ระบบ Offboarding (ขั้นตอนลาออก/เลิกจ้าง)
### 28.1 เช็กลิสต์ลาออก
- คืนอุปกรณ์: เสื้อ uniform, คีย์การ์ด, เครื่องมือ
- ตรวจสต็อก
- ปิดสิทธิการเข้าระบบ
- ส่งงาน

### 28.2 เอกสาร
- ใบลาออก
- ใบรับรองการทำงาน
- รายงานเงินคงค้าง (OT, วันลาที่เหลือ)

### 28.3 Audit
- เก็บประวัติการลาออกทั้งหมด
- ระบุเหตุผลการลาออก
- ใช้ทำ HR Analytics

---

## 29. ระบบเซ็นเอกสารออนไลน์ (e-Signature)
### 29.1 เอกสารที่เซ็นได้ผ่านระบบ
- สัญญาจ้าง
- ข้อตกลง OT
- ระเบียบพนักงาน
- ใบเตือน
- ใบชมเชย

### 29.2 ฟีเจอร์
- เซ็นบนมือถือผ่าน Web App
- เก็บ PDF พร้อมลายเซ็นมากกว่า 1 คน
- บันทึกเวลาที่เซ็น + IP Address + Device

---

## 30. ระบบ Workflow การอนุมัติ (Approval Flow System)
### 30.1 ประเภทคำขอที่อนุมัติได้
- ขอ OT
- ขอวันลา
- ขอเปลี่ยนกะ
- ขอเบิกอุปกรณ์
- แจ้งปัญหาเครื่องจักร
- เอกสาร HR อื่น ๆ

### 30.2 ความสามารถของ Workflow
- ตั้งลำดับหัวหน้าที่อนุมัติได้หลายระดับ
- ตั้ง SLA ว่าแต่ละคำขอควรได้รับการอนุมัติภายในกี่ชั่วโมง
- มีแจ้งเตือนเมื่อคำขอค้างนานเกินกำหนด

### 30.3 ระบบ TRACKING
- ใครอนุมัติแล้ว
- ใครยังไม่อนุมัติ
- เอกสารค้างอยู่ที่ใคร

---

## 31. สรุปภาพรวมระบบ HR ที่สมบูรณ์
เมื่อรวมทั้งหมดเข้าด้วยกัน ระบบนี้เทียบเท่า HR Platform มืออาชีพ แต่ปรับแต่งตรงตามความต้องการธุรกิจไทยได้ง่ายกว่า:
- Attendance
- OT
- Shift
- Leave
- Payroll
- KPI
- Document Management
- Offboarding
- Workflow
- LINE Integration
- Multi-Branch
- Face/GPS Verification

ระบบนี้สามารถขยายเป็น **Mini ERP ด้าน HR** ได้ในอนาคต
