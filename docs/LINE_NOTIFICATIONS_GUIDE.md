# 📱 คู่มือการใช้งาน LINE Notifications

ระบบแจ้งเตือนหลักของ Anajak HR ผ่าน LINE Messaging API

---

## ✅ ข้อดีของ LINE Notifications

1. **🌍 ครอบคลุมทุกแพลตฟอร์ม**
   - iOS, Android, Desktop, Web
   - ไม่ต้องติดตั้ง PWA
   - ไม่ต้องเปิด Browser

2. **🔔 Persistent & Reliable**
   - แจ้งเตือนถึงแม้ปิดเครื่อง
   - Server-sent notifications
   - ทำงานด้วย Cron Jobs อัตโนมัติ

3. **👥 คนไทยใช้กันทุกคน**
   - ไม่ต้องสอนวิธีใช้
   - มีอยู่แล้วในมือถือทุกคน

4. **🎨 Rich Messages**
   - รูปภาพ, สติ๊กเกอร์, Flex Messages
   - สวยงาม อ่านง่าย

5. **💰 ฟรี**
   - Free Plan: 500 messages/เดือน
   - เพียงพอสำหรับองค์กรขนาดเล็ก-กลาง

---

## 🔧 การตั้งค่า LINE API

### **ขั้นตอนที่ 1: สร้าง LINE Channel**

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. สร้าง Provider (ถ้ายังไม่มี)
3. สร้าง **Messaging API Channel**
4. กรอกข้อมูล:
   - Channel name: `Anajak HR`
   - Channel description: `ระบบแจ้งเตือนการทำงาน`
   - Category: `Business`

### **ขั้นตอนที่ 2: ตั้งค่า Channel**

1. ไปที่แทป **Messaging API**
2. คัดลอก **Channel Access Token** (long-lived)
   - ถ้ายังไม่มี คลิก `Issue` เพื่อสร้าง
3. เปิด **Webhooks** (ถ้าต้องการ)
4. ปิด **Auto-reply** และ **Greeting message** (แนะนำ)

### **ขั้นตอนที่ 3: หา Recipient ID**

**สำหรับ User ID:**
1. เพิ่มเพื่อน Official Account ที่สร้าง
2. ใช้ [LINE User ID Finder](https://lin.ee/ti/p/@your-channel)
3. หรือส่งข้อความหา Bot แล้วดู Webhook event

**สำหรับ Group ID:**
1. เชิญ Official Account เข้ากลุ่ม
2. ส่งข้อความในกลุ่ม
3. ดู Webhook event เพื่อเอา `groupId`

### **ขั้นตอนที่ 4: เพิ่มใน Admin Settings**

1. ไปที่ `/admin/settings/line`
2. วาง **Channel Access Token**
3. วาง **Recipient ID** (User ID หรือ Group ID)
4. กด **บันทึก**

---

## 🎯 การเปิดใช้งานการแจ้งเตือน

### **ไปที่ `/admin/settings/notifications`**

1. **เปิด Master Switch:**
   ```
   ☑️ เปิดการแจ้งเตือน
   ```

2. **เลือกประเภทการแจ้งเตือน:**
   ```
   LINE Notifications:
   ☑️ แจ้งเตือนเมื่อเช็คอิน
   ☑️ แจ้งเตือนเมื่อเช็คเอาท์
   ☑️ แจ้งเตือนคำขอ OT
   ☑️ แจ้งเตือนการอนุมัติ OT
   ☑️ แจ้งเตือนเริ่มทำ OT
   ☑️ แจ้งเตือนจบ OT
   ☑️ แจ้งเตือนวันหยุด
   ☑️ แจ้งเตือน Auto Checkout
   ☑️ เตือนลืมเช็คเอาท์
   ```

3. **ตั้งค่าวันหยุด:**
   ```
   แจ้งล่วงหน้า: [ 1 ] วัน
   (ตั้งได้ 1-7 วัน)
   ```

4. **กดบันทึก** ✅

---

## 📊 รายการแจ้งเตือนทั้งหมด

### **⏰ Check-in / Check-out**

| เหตุการณ์ | เงื่อนไข | API Endpoint |
|---------|---------|-------------|
| เช็คอิน | เมื่อพนักงาน check-in | `/api/checkin-notification` |
| เช็คอิน (สาย) | เมื่อ check-in หลังเวลาเข้างาน | `/api/checkin-notification` |
| เช็คเอาท์ | เมื่อพนักงาน check-out | `/api/checkout-notification` |
| เตือนลืมเช็คเอาท์ | ก่อน auto checkout 30 นาที | API route (OT/Late) |
| Auto Checkout | ระบบเช็คเอาท์ให้อัตโนมัติ | Cron (22:00 daily) |

### **💼 OT (Overtime)**

| เหตุการณ์ | เงื่อนไข | API Endpoint |
|---------|---------|-------------|
| คำขอ OT ใหม่ | เมื่อพนักงานขอ OT | `/api/notifications` |
| OT อนุมัติ/ปฏิเสธ | เมื่อแอดมินอนุมัติ/ปฏิเสธ | `/api/notifications` |
| เริ่มทำ OT | เมื่อพนักงาน start OT | `/api/notifications` |
| จบ OT | เมื่อพนักงาน end OT | `/api/notifications` |

### **🏖️ ลา / WFH / Field Work**

| เหตุการณ์ | เงื่อนไข | API Endpoint |
|---------|---------|-------------|
| คำขอลา | เมื่อพนักงานขอลา | `/api/notifications` |
| ลาอนุมัติ/ปฏิเสธ | เมื่อแอดมินอนุมัติ/ปฏิเสธ | `/api/notifications` |
| คำขอ WFH | เมื่อพนักงานขอ WFH | `/api/notifications` |
| WFH อนุมัติ/ปฏิเสธ | เมื่อแอดมินอนุมัติ/ปฏิเสธ | `/api/notifications` |
| คำขอ Field Work | เมื่อพนักงานขอ Field Work | `/api/notifications` |

### **🎉 วันหยุด**

| เหตุการณ์ | เงื่อนไข | Cron Schedule |
|---------|---------|--------------|
| แจ้งล่วงหน้า | N วันก่อนวันหยุด | 09:00 ทุกวัน |
| แจ้งวันหยุดวันนี้ | ในวันหยุด | 09:00 ทุกวัน |

---

## 🔍 การทดสอบ

### **วิธีที่ 1: ทดสอบผ่าน UI**
1. เช็คอิน/เช็คเอาท์ จริงๆ
2. สร้างคำขอ OT, ลา, WFH
3. อนุมัติ/ปฏิเสธคำขอ

### **วิธีที่ 2: ทดสอบผ่าน API** (ใช้ Postman)

```bash
# Test holiday notification
POST https://anajak-hr.vercel.app/api/holiday-notifications
Headers: Authorization: Bearer <secret>

# Test auto checkout
POST https://anajak-hr.vercel.app/api/auto-checkout
Headers: Authorization: Bearer <secret>
```

### **วิธีที่ 3: ตรวจสอบ Logs**

```bash
# Vercel Dashboard
https://vercel.com/your-team/anajak-hr/logs

# กรองด้วย:
[Holiday Notifications]
[Auto Checkout]
[LINE Notification]
```

---

## ⚠️ Troubleshooting

### **ไม่ได้รับการแจ้งเตือน**

1. ✅ ตรวจสอบว่า **Channel Access Token** ถูกต้อง
2. ✅ ตรวจสอบว่า **Recipient ID** ถูกต้อง
3. ✅ ตรวจสอบว่า **เปิดการแจ้งเตือน** ใน Admin Settings
4. ✅ ตรวจสอบว่า **เปิดการแจ้งเตือนแต่ละประเภท**
5. ✅ ตรวจสอบว่าเพิ่มเพื่อน Official Account แล้ว
6. ✅ ตรวจสอบใน Vercel Logs

### **ข้อความส่งไม่ออก (Error 401)**

- **สาเหตุ:** Token หมดอายุหรือไม่ถูกต้อง
- **แก้ไข:** Issue token ใหม่จาก LINE Developers Console

### **ข้อความส่งไม่ออก (Error 400)**

- **สาเหตุ:** Recipient ID ไม่ถูกต้อง หรือยังไม่ได้เพิ่มเพื่อน
- **แก้ไข:** ตรวจสอบ User ID/Group ID อีกครั้ง

### **Cron Jobs ไม่ทำงาน**

- **สาเหตุ:** Vercel cron ยังไม่ได้ถูก trigger
- **แก้ไข:** 
  1. ตรวจสอบ `vercel.json` มี cron configuration
  2. Deploy ใหม่
  3. รอจนถึงเวลาที่กำหนด (09:00 หรือ 22:00)

---

## 📈 Quota & Limitations

### **LINE Free Plan:**
- 500 messages/เดือน
- เพียงพอสำหรับองค์กร ~20-30 คน

### **การคำนวณ:**
```
20 พนักงาน × 2 ข้อความ/วัน (เช็คอิน+เช็คเอาท์) × 22 วันทำงาน 
= 880 messages/เดือน

➡️ ต้องอัปเกรดเป็น Basic Plan (400 บาท/เดือน)
```

### **LINE Basic Plan:**
- 📊 Push API: 4,000 messages/เดือน (แถมฟรี)
- 📊 Broadcast: ฟรี 500 messages/เดือน
- 💰 ราคา: 400 บาท/เดือน

---

## 🎉 สรุป

LINE Notifications คือทางเลือกที่ดีที่สุดสำหรับองค์กรไทย:

✅ **เสถียร** - Server-sent, ไม่พึ่ง Browser  
✅ **ครอบคลุม** - ทุก platform, ทุกคน  
✅ **คุ้นเคย** - ใช้ LINE อยู่แล้ว  
✅ **ราคาเหมาะสม** - ฟรี หรือ 400 บาท/เดือน  

---

**💡 Tips:** 
- ใช้ **Group Chat** เพื่อให้ทีมงานเห็นการแจ้งเตือนพร้อมกัน
- ปิด **Auto-reply** ใน LINE Official Account settings เพื่อไม่ให้รบกวน
- ตั้งชื่อ Official Account ให้ชัดเจน เช่น `Anajak HR Bot`

---

📚 **Related Docs:**
- `NOTIFICATION_SYSTEMS.md` - เปรียบเทียบ LINE vs PWA
- `/admin/settings/line` - หน้าตั้งค่า LINE API
- `/admin/settings/notifications` - หน้าตั้งค่าการแจ้งเตือน

