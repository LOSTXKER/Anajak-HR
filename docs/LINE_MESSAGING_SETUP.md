# LINE Messaging API Setup Guide

## 📱 การตั้งค่า LINE Messaging API สำหรับระบบแจ้งเตือน

ระบบนี้ใช้ **LINE Messaging API** ในการส่งการแจ้งเตือนไปยัง LINE ของพนักงาน

## 🚀 ตั้งค่าผ่านหน้าเว็บ (แนะนำ)

✨ **ตั้งค่าได้ง่ายๆ ผ่านหน้าเว็บ ไม่ต้องแก้ไข .env.local!**

1. ทำตามขั้นตอนด้านล่างเพื่อรับ **Channel Access Token** และ **User/Group ID**
2. เข้าหน้า **Admin > ตั้งค่า** (`/admin/settings`)
3. เลื่อนลงมาที่ส่วน **LINE Messaging API**
4. วาง Token และ ID ที่ได้มา
5. คลิก **"ทดสอบส่งข้อความ LINE"** เพื่อตรวจสอบ
6. คลิก **"บันทึกการตั้งค่า"**

เสร็จแล้ว! ไม่ต้องแก้ไขโค้ดหรือ .env.local เลย 🎉

---

## 🔑 ขั้นตอนการสร้าง LINE Channel

### 1. เข้าสู่ LINE Developers Console
ไปที่: https://developers.line.biz/console/

### 2. Login ด้วย LINE Account
- ใช้ LINE Account ของบริษัทหรือองค์กร

### 3. สร้าง Provider (ครั้งแรก)
1. คลิก **"Create a new provider"**
2. ตั้งชื่อ Provider เช่น `Anajak Company`
3. คลิก **"Create"**

### 4. สร้าง Channel (Messaging API)
1. ในหน้า Provider คลิก **"Create a new channel"**
2. เลือก **"Messaging API"**
3. กรอกข้อมูล:
   - **Channel type**: Messaging API
   - **Provider**: เลือก Provider ที่สร้างไว้
   - **Channel icon**: อัปโหลดโลโก้บริษัท
   - **Channel name**: `Anajak HR Bot`
   - **Channel description**: `ระบบแจ้งเตือนการทำงาน`
   - **Category**: Business
   - **Subcategory**: Enterprise
   - **Email address**: อีเมลผู้ดูแล
4. ยอมรับเงื่อนไข
5. คลิก **"Create"**

---

## 🔐 รับ Channel Access Token

### 1. เข้าสู่หน้า Channel Settings
1. ไปที่ Providers > เลือก Provider > เลือก Channel
2. ไปที่แท็บ **"Messaging API"**

### 2. Issue Channel Access Token
1. หาหัวข้อ **"Channel access token"**
2. คลิก **"Issue"**
3. **คัดลอก Token** ที่แสดงขึ้นมา

⚠️ **สำคัญ:** เก็บ Token ไว้ในที่ปลอดภัย อย่าแชร์ให้ใครทราบ

---

## 👤 เพิ่มบอทเป็นเพื่อน

### วิธีที่ 1: สแกน QR Code
1. ในแท็บ **"Messaging API"** หา QR Code
2. เปิด LINE App แล้วสแกน QR Code
3. เพิ่มบอทเป็นเพื่อน

### วิธีที่ 2: ค้นหา Bot ID
1. หา **Bot basic ID** (เช่น `@123abcde`)
2. เปิด LINE App > ค้นหา > พิมพ์ Bot ID
3. เพิ่มบอทเป็นเพื่อน

---

## 🔍 หา User ID หรือ Group ID

### สำหรับการแจ้งเตือนแบบ 1-on-1 (User ID)

**วิธีที่ 1: ผ่าน LINE Official Account Manager**
1. ส่งข้อความหาบอท
2. ไปที่ https://manager.line.biz/
3. เลือกบอทของคุณ
4. ไปที่ **Settings > Response settings**
5. จะเห็น User ID

**วิธีที่ 2: ใช้ Webhook Script**

สร้างไฟล์ `webhook-test.js`:

```javascript
const express = require('express');
const app = express();

app.use(express.json());

app.post('/webhook', (req, res) => {
  console.log('📥 Webhook Event:', JSON.stringify(req.body, null, 2));
  
  const events = req.body.events || [];
  events.forEach(event => {
    if (event.source) {
      console.log('👤 Source Type:', event.source.type);
      console.log('🆔 ID:', event.source.userId || event.source.groupId);
    }
  });
  
  res.json({ success: true });
});

app.listen(3001, () => {
  console.log('✅ Webhook listener: http://localhost:3001/webhook');
});
```

รันแล้วส่งข้อความหาบอท จะเห็น User ID ใน console

### สำหรับการแจ้งเตือนกลุ่ม (Group ID) - **แนะนำสำหรับองค์กร**

1. สร้างกลุ่ม LINE ใหม่
2. เชิญบอทเข้ากลุ่ม (สแกน QR หรือค้นหา Bot ID)
3. ส่งข้อความในกลุ่ม
4. ดู webhook event จะเห็น `groupId`
5. คัดลอก Group ID

**รูปแบบ ID:**
- User ID: ขึ้นต้นด้วย `U` (เช่น `U1234567890abcdef...`)
- Group ID: ขึ้นต้นด้วย `C` (เช่น `C1234567890abcdef...`)
- Room ID: ขึ้นต้นด้วย `R`

---

## ⚙️ ตั้งค่าในหน้าเว็บ

1. เข้าหน้า **Admin > ตั้งค่า** (`/admin/settings`)
2. เลื่อนลงมาที่ **LINE Messaging API**
3. กรอกข้อมูล:
   - **Channel Access Token**: วาง Token จากขั้นตอนที่ผ่านมา
   - **ประเภทผู้รับ**: เลือก "User ID (1-on-1)" หรือ "Group ID (กลุ่ม)"
   - **LINE User/Group ID**: วาง ID ที่ได้มา
4. คลิก **"ทดสอบส่งข้อความ LINE"**
   - ถ้าสำเร็จจะเห็นข้อความทดสอบใน LINE
   - ถ้าไม่สำเร็จตรวจสอบ Token และ ID
5. เมื่อทดสอบสำเร็จแล้ว คลิก **"บันทึกการตั้งค่า"**

---

## 🧪 ทดสอบระบบ

ระบบจะส่งการแจ้งเตือนอัตโนมัติเมื่อ:
- ✅ OT ได้รับการอนุมัติ/ปฏิเสธ
- ✅ ใบลาได้รับการอนุมัติ/ปฏิเสธ  
- ✅ WFH ได้รับการอนุมัติ/ปฏิเสธ

ลองอนุมัติคำขอ OT ดูว่าได้รับการแจ้งเตือนใน LINE หรือไม่!

---

## 🐛 แก้ปัญหา

### "Invalid access token"
- ตรวจสอบว่าคัดลอก Token ครบหรือไม่
- ลองสร้าง Token ใหม่
- ตรวจสอบว่าวางใน Settings ถูกต้อง

### "Recipient not found"
- บอทถูกบล็อกโดยผู้ใช้
- ยังไม่ได้เพิ่มบอทเป็นเพื่อน
- สำหรับกลุ่ม: บอทถูกเตะออกจากกลุ่ม
- ตรวจสอบรูปแบบ ID (U สำหรับ user, C สำหรับ group)

### ไม่ได้รับข้อความ
- ตรวจสอบว่าเปิด "เปิดการแจ้งเตือน" ใน Settings หรือไม่
- ตรวจสอบ webhook ใน LINE Developers Console
- ดู error logs ในแท็บ Messaging API

### "The request body has error(s)"
- ID ไม่ถูกต้อง
- Token หมดอายุหรือไม่ถูกต้อง
- ตรวจสอบประเภทผู้รับให้ตรงกับ ID

---

## 📚 เอกสารเพิ่มเติม

- [LINE Messaging API Documentation](https://developers.line.biz/en/docs/messaging-api/)
- [LINE Developers Console](https://developers.line.biz/console/)
- [LINE Official Account Manager](https://manager.line.biz/)

---

## 🔄 Migration จาก LINE Notify

ถ้าเคยใช้ LINE Notify:

1. ❌ ลบ `LINE_NOTIFY_TOKEN` จาก `.env.local` (ถ้ามี)
2. ❌ ลบ `lib/line/notify.ts` (ลบแล้ว)
3. ✅ ทำตามขั้นตอนข้างบนเพื่อตั้งค่า LINE Messaging API
4. ✅ ตั้งค่าผ่านหน้า Admin > Settings
5. ✅ ทดสอบและบันทึก!

LINE Messaging API ใช้งานง่ายกว่า มีฟีเจอร์เยอะกว่า และไม่มีปัญหา token หมดอายุ! 🎉
